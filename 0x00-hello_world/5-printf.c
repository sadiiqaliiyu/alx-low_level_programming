#include <stdio.h>
/**
 *main -This our main function were program is run
 *@void: this is the parameter value of main
 *Return: this always return the value 0, signifying success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
