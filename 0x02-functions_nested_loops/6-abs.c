#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * @1: input number as an integer
 *
 * Return; absolute value
 */
int _abs(int 1)
{
	if (i >= 0)
	{
		return(i);
	}
	else
	{
		return(i * -1);
	}
}
